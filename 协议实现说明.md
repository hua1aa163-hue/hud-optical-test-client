# HUD 通信协议 V3.1 实现说明

本工程依据 `HUD通信协议_V3.1.docx` 实现。协议正文没有固定长度、字节偏移、长度头或 CRC；请求为文本指令，光学软件是 TCP 服务器，返回以 `%` 结束。

## 连接与拆包

- 客户端默认连接 `127.0.0.1:5556`，地址和端口可编辑。
- 默认使用无 BOM 的 UTF-8；协议原文未声明编码。
- 接收缓存以 `%` 拆分完整返回，因此支持 TCP 半包和粘包。
- 单条未完成返回最多保留 1 MiB，防止异常服务器无限占用内存。
- 主动断开、远端断开、发送异常和重连替换都会结束当前连接代次，并取消正在运行的批量联测。

## 可编辑指令目录

### 光学测试

`t1`～`t21` 全部内置。普通项目直接发送对应代码；`t11` 支持两种形式：

```text
t11
t11/{X平移距离}/{Y平移距离}
```

X、Y 必须同时为空或同时填写。界面同时显示测试含义、发送内容、动态字段、返回格式和按顺序排列的返回字段含义；原始发送内容始终可直接覆盖编辑。

### 辅助指令

```text
c-{配方名}
gin
pic-{完整文件路径}
bmp-{目录}|{名称}
n-{文件名},%
```

## 返回判定

- 测试命令成功返回应以当前请求对应的 `..._Result` 开头，并以 `%` 结束。
- 仅 `t5` 把协议明确定义的 `Error0%`、`Error1%` 判为失败；所有光学测试把通用终态 `Fail%`、`NG%` 判为失败。其他未定义的 `ErrorN%` 不会被臆测为失败。
- 辅助指令使用 `OK%` / `Fail%`；返回本身不带命令标识，因此会关联到当前正在等待的辅助指令。保存数据的文档只明确了 `OK%`，实现仍把 `Fail%` 作为客户端失败保护。
- 光学测试默认等待 120 秒，辅助指令默认等待 10 秒。
- 与当前请求无关的返回会继续等待，不会误判为完成。

## 原文歧义（界面中保留提示）

- `t1` 文字称 18 项，示例只有 17 项。
- `t2` 文字称 17 项，示例只有 13 项。
- `t7`、`t12`、`t13` 的“色差”组三个值没有逐项定义。
- `t14` 前 11 组是三元组，最后两个色域值是标量。
- `t19` 汇总组只有第一个值有明确定义。
- `t20` 示例缺少若干空格，不能作为权威默认数据。
- `t21` 说明文字误写为 `t3_Result`；实现优先按请求匹配 `t21_Result`，并仅在等待 `t21` 时兼容 `t3_Result`。
- 除 `t11` 的 mrad 和明确标为像素的字段外，多数字段单位未定义，界面不会自行猜测单位。

这些矛盾不会被静默“修正”为虚构规则。编辑器展示规范字段，并把文档冲突作为操作提示；实际原始返回始终完整记录在通信日志中。
